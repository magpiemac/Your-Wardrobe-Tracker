<strong><%= @capsule.name %></strong>

<%= link_to 'Edit', edit_capsule_path(@capsule) %> |
<%= link_to 'Back', capsules_path %>

<% @capsule.wardrobe_items.each do |wardrobe_item| %>
     <%= link_to "#{wardrobe_item.item} - #{wardrobe_item.description}", wardrobe_items_path(:id) %>
 <% end %>
